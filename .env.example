# ── Deployment secrets ────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env — it is listed in .gitignore.

# Private key of the deployer wallet (without 0x prefix).
# Use a dedicated testnet wallet; never use a mainnet wallet here.
PRIVATE_KEY=your_private_key_here

# ── RPC endpoints ─────────────────────────────────────────────────────────────
# Get a free Sepolia RPC URL from Alchemy (https://alchemy.com) or
# Infura (https://infura.io) after creating a free account.
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/your_api_key_here

# ── Etherscan ─────────────────────────────────────────────────────────────────
# Used by `forge script --verify` to submit source code to Etherscan.
# Get a free API key at https://etherscan.io/myapikey
ETHERSCAN_API_KEY=your_etherscan_api_key_here
